{
  // 测试基座配置：Authority 刻意指向保留 TLD ".invalid"——该域名永远无法解析，
  // 测试若意外走到真实认证会快速失败，防止用例误连真实环境。
  // 需要真实 AuthServer 的集成测试在各自 appsettings.secrets.json / 环境变量里覆盖。
  "AuthServer": {
    "Authority": "https://authserver-test.invalid"
  }
}
